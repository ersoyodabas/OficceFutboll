// Presentation metadata only; the server selects the scoring team.
export const TEAMS = Object.freeze({
  blue: Object.freeze({ id: 'blue', name: 'MAVİ FC', initials: 'MFC', logo: 'teams/team-blue.svg', color: '#56b6ff' }),
  red: Object.freeze({ id: 'red', name: 'KIRMIZI FC', initials: 'KFC', logo: 'teams/team-red.svg', color: '#ff657d' }),
});
